$(document).ready(function(){
  console.log('hey')
  $('.your-class').slick({
    // setting-name: 'setting-value',
    autoplay:true,
    autoplaySpeed:3000,
    arrows:true,
    dots:true
  });

});